# Optional local settings. Copy to .env.local; never commit it.
# Logins are no longer stored here: the sync opens Chrome and you log in by hand once.
# ROLL_CALL_CDP_URL=http://127.0.0.1:9222   # reuse a Chrome started with --remote-debugging-port
